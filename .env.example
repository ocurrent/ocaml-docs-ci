# Copy to .env. docker-compose reads .env automatically.
UID=1000
GID=1000
# GID of the host's [docker] group (see: getent group docker), so the
# daemon can reach the mounted /var/run/docker.sock. Often 999 or 998.
DOCKER_GID=999
