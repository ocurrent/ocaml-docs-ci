(** Basics 2.0.0 — deliberately broken. The body below has a
    syntax error so dune cannot compile the source. Used as a
    regression probe: basics.1.0.0 (in a prior commit) should
    stay green, basics.2.0.0 should fail. *)

let x = ;
