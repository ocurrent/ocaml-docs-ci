(** {!Xref_forward} (1.0.0) — canary for unresolved xrefs. The
    reference to {!Later.t} below points at a module that does not
    exist in this version. Odoc emits a warning and renders the
    link as an [xref-unresolved] span. Fixed in 1.0.1. *)

(** See {!Later.t} for the downstream type (NOT YET DEFINED). *)
type t = int
